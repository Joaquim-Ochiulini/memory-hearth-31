/**
 * services/ — infrastructure adapters (Supabase, storage, analytics…).
 *
 * Every external side-effect goes through a service so features stay
 * declarative and testable. The Supabase client will live in
 * services/supabase/client.ts once Cloud is enabled.
 */
export {};
